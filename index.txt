import os

# Contenu de l'application (HTML, CSS et JS combinés pour la simplicité de déploiement)
app_html = """
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>La Méthode Fréquence — Agnès & Vous</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-cyan: #00e5ff;
            --primary-violet: #7c4dff;
            --bg-light: #fdfbff;
            --text-dark: #2c3e50;
            --accent-gold: #d4af37;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: var(--bg-light);
            color: var(--text-dark);
            margin: 0;
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* --- Page de Capture (Gate) --- */
        #gate {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(135deg, var(--primary-cyan), var(--primary-violet));
            display: flex; justify-content: center; align-items: center;
            z-index: 1000; transition: transform 0.8s cubic-bezier(0.7, 0, 0.3, 1);
        }

        .capture-card {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
            text-align: center;
            max-width: 400px;
            width: 90%;
        }

        .capture-card h1 { font-family: 'Playfair Display', serif; color: var(--primary-violet); margin-bottom: 10px; }
        
        input[type="email"] {
            width: 100%;
            padding: 12px;
            margin: 20px 0;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-sizing: border-box;
        }

        .btn-enter {
            background: var(--primary-violet);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 50px;
            cursor: pointer;
            font-weight: 600;
            transition: 0.3s;
        }

        .btn-enter:hover { transform: scale(1.05); background: var(--primary-cyan); color: var(--text-dark); }

        /* --- App Principal --- */
        header {
            padding: 20px;
            text-align: center;
            background: white;
            border-bottom: 2px solid var(--primary-cyan);
        }

        .logo { font-family: 'Playfair Display', serif; font-size: 24px; font-weight: 700; }

        .mode-toggle {
            display: flex; justify-content: center; gap: 20px; margin: 30px 0;
        }

        .btn-mode {
            padding: 10px 20px;
            border: 2px solid var(--primary-violet);
            background: transparent;
            border-radius: 20px;
            cursor: pointer;
            font-weight: 600;
        }

        .btn-mode.active { background: var(--primary-violet); color: white; }

        /* --- La Balle de Cohérence --- */
        .coherence-container {
            height: 300px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: rgba(124, 77, 255, 0.05);
            margin: 20px auto;
            border-radius: 100%;
            width: 300px;
            position: relative;
        }

        .ball {
            width: 50px; height: 50px;
            background: radial-gradient(circle at 30% 30%, var(--primary-cyan), var(--primary-violet));
            border-radius: 50%;
            box-shadow: 0 0 30px var(--primary-cyan);
            animation: breathe 10s infinite ease-in-out;
            animation-play-state: paused;
        }

        @keyframes breathe {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(3); }
        }

        .instruction-text { margin-top: 20px; font-weight: 600; color: var(--primary-violet); }

        /* --- Audio Select --- */
        .audio-controls { text-align: center; margin: 20px 0; }
        select { padding: 8px; border-radius: 10px; border: 1px solid var(--primary-violet); }

        /* --- Étapes du Protocole --- */
        .step-card {
            max-width: 800px;
            margin: 20px auto;
            padding: 25px;
            background: white;
            border-left: 5px solid var(--primary-cyan);
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .step-title { font-weight: 600; color: var(--primary-violet); text-transform: uppercase; font-size: 0.9em; }
        .step-name { font-family: 'Playfair Display', serif; font-size: 1.5em; margin: 5px 0; }

        .hidden { display: none; }
    </style>
</head>
<body>

    <div id="gate">
        <div class="capture-card">
            <h1>Bienvenue</h1>
            <p>Entrez votre email pour accéder à votre recalibration quantique.</p>
            <input type="email" id="userEmail" placeholder="votre@email.com" required>
            <button class="btn-enter" onclick="unlock()">Accéder à la Méthode</button>
        </div>
    </div>

    <header>
        <div class="logo">La Méthode Fréquence — <span style="color: var(--primary-violet)">Agnès & Vous</span></div>
    </header>

    <div class="container">
        <div class="mode-toggle">
            <button id="btn27" class="btn-mode active" onclick="switchMode(27)">27 MINUTES (Complet)</button>
            <button id="btn9" class="btn-mode" onclick="switchMode(9)">9 MINUTES (Express)</button>
        </div>

        <div class="audio-controls">
            <label>Ambiance sonore : </label>
            <select id="audioSelect" onchange="changeAudio()">
                <option value="none">Silence</option>
                <option value="waves">Vagues de l'océan</option>
                <option value="rain">Pluie apaisante</option>
                <option value="wind">Vent des sommets</option>
            </select>
            <audio id="bgAudio" loop></audio>
        </div>

        <div id="coherenceZone">
            <div class="coherence-container">
                <div class="ball" id="ball"></div>
            </div>
            <center>
                <div class="instruction-text" id="breathText">Prêt à commencer ?</div>
                <button class="btn-enter" style="margin-top:10px" onclick="toggleBreathe()">Démarrer la Cohérence</button>
            </center>
        </div>

        <div id="stepsArea"></div>
    </div>

    <script>
        const stepsData = {
            27: [
                { id: "1", title: "Ancrage Vibratoire", time: "6 min", desc: "Cohérence cardiaque d'intention. Calibrez votre champ électromagnétique sur la fréquence désirée." },
                { id: "2", title: "Projection Neuronale", time: "8 min", desc: "Câblage de nouveaux circuits. Visualisez 4 scènes précises avec une intensité sensorielle totale." },
                { id: "3", title: "Lecture des Signaux", time: "5 min", desc: "Programmez votre SRA (Système Réticulaire Activateur) à filtrer et trouver les preuves dans votre journée." },
                { id: "4", title: "Incarnation Expansive", time: "7 min", desc: "Incarnez l'identité. Comment pense-t-elle ? Quelles décisions prend-elle aujourd'hui ?" },
                { id: "5", title: "Le Scellement", time: "1 min", desc: "Saturez les dernières 60 secondes par répétition haute fréquence." }
            ],
            9: [
                { id: "1", title: "Ancrage Vibratoire Express", time: "3 min", desc: "Cohérence ciblée sur une seule scène, une seule émotion." },
                { id: "2", title: "Fusion Identitaire", time: "5 min", desc: "Lois 2 et 3 fusionnées. Posture, respiration et regard de la version expansive." },
                { id: "3", title: "Le Scellement", time: "1 min", desc: "Répétition intense de l'intention principale." }
            ]
        };

        const audioFiles = {
            waves: "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3", // Remplacer par vos liens réels
            rain: "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-2.mp3",
            wind: "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-3.mp3"
        };

        function unlock() {
            const email = document.getElementById('userEmail').value;
            if(email.includes('@')) {
                localStorage.setItem('mf_email', email);
                // Ici vous pouvez ajouter un appel vers Google Sheets / Google Apps Script
                document.getElementById('gate').style.transform = 'translateY(-100%)';
            } else {
                alert("Veuillez entrer un email valide.");
            }
        }

        function switchMode(mode) {
            document.getElementById('btn27').classList.toggle('active', mode === 27);
            document.getElementById('btn9').classList.toggle('active', mode === 9);
            renderSteps(mode);
        }

        function renderSteps(mode) {
            const area = document.getElementById('stepsArea');
            area.innerHTML = '';
            stepsData[mode].forEach(step => {
                area.innerHTML += `
                    <div class="step-card">
                        <div class="step-title">Étape ${step.id} — ${step.time}</div>
                        <div class="step-name">${step.title}</div>
                        <p>${step.desc}</p>
                    </div>
                `;
            });
        }

        let isBreathing = false;
        function toggleBreathe() {
            const ball = document.getElementById('ball');
            const txt = document.getElementById('breathText');
            isBreathing = !isBreathing;
            
            if(isBreathing) {
                ball.style.animationPlayState = 'running';
                txt.innerText = "Inspirez (5s) ... Expirez (5s)";
            } else {
                ball.style.animationPlayState = 'paused';
                txt.innerText = "Session en pause";
            }
        }

        function changeAudio() {
            const select = document.getElementById('audioSelect');
            const audio = document.getElementById('bgAudio');
            if(select.value === 'none') {
                audio.pause();
            } else {
                audio.src = audioFiles[select.value];
                audio.play();
            }
        }

        // Init
        if(localStorage.getItem('mf_email')) document.getElementById('gate').classList.add('hidden');
        renderSteps(27);
    </script>
</body>
</html>
"""

# Créer le répertoire et le fichier
if not os.path.exists('app_frequence'):
    os.makedirs('app_frequence')

with open('app_frequence/index.html', 'w', encoding='utf-8') as f:
    f.write(app_html)

print("Fichier index.html généré avec succès.")